# Example: MaxPilot with room temperature sensor and climate control.
# The temperature is imported from an existing HA sensor entity
# (Zigbee, BLE, WiFi, etc.) and a thermostat climate entity automatically
# switches between Confort and Éco based on room temperature.
#
# Exemple : MaxPilot avec capteur de température et contrôle climatique.
# La température est importée depuis une entité HA existante
# (Zigbee, BLE, WiFi, etc.) et une entité thermostat bascule automatiquement
# entre Confort et Éco selon la température de la pièce.

substitutions:
  name: maxpilot_ch1
  ch_name: "Salon"
  ssr1_pin: D3
  ssr2_pin: D7
  uptime_name: "Uptime Sensor"
  # Replace with your actual HA temperature sensor entity ID
  # Remplacez par l'entity ID de votre capteur de température HA
  temp_sensor_entity: "sensor.temperature_salon"
  temp_sensor_internal: "false"

packages:
  core: !include common/core.yaml
  wifi: !include common/wifi.yaml
  maxpilot: !include common/maxpilot.yaml
  climate: !include common/maxpilot_climate.yaml

esphome:
  name: ${name}
